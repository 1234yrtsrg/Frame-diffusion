# Core dependencies for Express4D blendshape interpolation.
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.5.1+cu121
numpy
pandas
pyyaml
tqdm
matplotlib

# Optional diffusion backend when diffusion.is_linear is enabled.
linear_attention_transformer

# Optional: used by cubic_interpolation baseline when available.
scipy
